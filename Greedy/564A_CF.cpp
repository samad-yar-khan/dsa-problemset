#include<bits/stdc++.h>
using namespace std;



int main(){

    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int n = 0 ;
    cin>>n;

    vector<int> vec;
    for(int i = 0 ; i < n ; i++){
        int a = 0 ;
        cin>>a;
        vec.push_back(a);
    }

    vector<int> st ;
    
    for(int i = 0 ; i < n ; i++){
        int a = 0 ;
        cin>>a;
        st.push_back(a);
    }

    int si = 0 ;
    int ei = 2*n ; // max operations
    int ans = 2*n;

    while(si<=ei){



    }

    return 0;
}
